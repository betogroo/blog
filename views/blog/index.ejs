<%- include('../partials/header.ejs') %>

<div class="container">

    <h1>Página inicial do Blog</h1>

    <% posts.forEach(post => { %>
    <h4><%= `[${post.id}] - ${post.title}` %> </h4>
    <p><%= post.message %> </p>
    <h6><%= post.author %> </h6>
    <h3>Respostas</h3>
    <% post.response.forEach(response => { %>
    <p><%= response.message %> </p>
    <h6><%= response.author %> </h6>
    <% }) %>
    <hr>
    <% }) %>



</div>

<%- include('../partials/footer.ejs') %>